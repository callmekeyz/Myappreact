// 1. import React
import React from 'react';

// 3. define the Header!
function Header() {
    // always always return some JSX!
    return (
        <header>
            <h1>Dad Joke App!</h1>
            <p>&copy; 2021 radishmouse, llc</p>
        </header>
    );
}

// 2. export default Header
export default Header;